name: offline_web_proxy_example
description: Example application demonstrating offline_web_proxy usage
version: 1.0.0+1
publish_to: none  # This is an example, not a published package

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  
  # Use the offline_web_proxy package
  offline_web_proxy:
    path: ../
  
  # Additional dependencies for the example
  http: ^1.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: 4.0.0

flutter:
  uses-material-design: true